<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.businesstarget2.dao.BusinessTarget2Dao">
	
	<!-- 查询数据
	<select id="findList" resultType="BusinessTarget2">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

    <select id="findIn" resultType="com.jeesite.modules.businesstarget2.entity.BusinessTarget2">
        SELECT ${sqlMap.column.toSql()}
        FROM ${sqlMap.table.toSql()}
        where id in
        <foreach collection="list" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
        ORDER BY ${sqlMap.order.toSql()}
    </select>
</mapper>