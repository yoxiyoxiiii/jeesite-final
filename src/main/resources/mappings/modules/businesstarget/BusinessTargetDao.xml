<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.businesstarget.dao.BusinessTargetDao">
	
	<!-- 查询数据
	<select id="findList" resultType="BusinessTarget">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

    <select id="findList" resultType="com.jeesite.modules.businesstarget.entity.BusinessTarget">
       SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
    </select>
    <select id="findListByCheckTemplateId" resultType="com.jeesite.modules.businesstarget.entity.BusinessTarget">
        SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
    </select>
</mapper>